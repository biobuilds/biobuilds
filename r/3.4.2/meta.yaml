{% set version = "3.4.2" %}

package:
  name: r
  version: "{{ version }}"

build:
  skip: True    # [win]
  number: 0

requirements:
  run:
    - r-base =={{ version }}
    - r-recommended =={{ version }}

about:
  home: https://www.r-project.org/
  license: GPL
  summary: "Language and environment for statistical computing (meta-package)"

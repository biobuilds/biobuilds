libpng:
  - "1.6"

libtiff:
  - "4.0"

jpeg:
  - "9b"

freetype:
  - "2.8"

# Build using older zlib version to avoid compatibility breaks with existing
# BioBuilds packages that depend on libgd.
zlib:
  - "1.2.8"

pin_run_as_build:
  jpeg:
    max_pin: "x"

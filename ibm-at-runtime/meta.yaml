{% set version = "10.0" %}

package:
  name: ibm-at-runtime
  version: {{ version }}

requirements:
  build: []
  run: []

build:
  skip: True  # [not ppc64le]
  number: 1

about:
  summary: "Package that checks for the presence of the IBM AT runtime"

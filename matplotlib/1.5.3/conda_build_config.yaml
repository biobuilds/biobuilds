# Force a specific cairo version for compatibility with other packages
# (primiarly R) in the planned 2017.11 release
cairo:
  - 1.14.10

numpy:
  - 1.13.3

tk:
  - 8.6

pin_run_as_build:
  numpy:
    min_pin: "x.x"
    max_pin: "x.x"
  tk:
    max_pin: "x.x"
